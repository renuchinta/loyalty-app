spring:
  datasource:
    url: jdbc:mysql://loyaltymysql:3306/LOYALTY_APP
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8090
